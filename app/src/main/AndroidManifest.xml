<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 🔹 QUYỀN CAMERA & FEATURE (từ master) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Finalapp"
        tools:targetApi="31">

        <!-- Activity chính -->
        <activity
            android:name="com.FinalProject.mainActivity.MainActivity"
            android:exported="true" />
        
        <!-- Home Organizer - không phải launcher -->
        <activity 
            android:name="com.FinalProject.feature_home_organizer.presentation.HomeOrganizerActivity"
            android:exported="false"
            tools:replace="android:exported" />

        <!-- Login Activity - LAUNCHER chính của app -->
        <activity
            android:name="com.FinalProject.feature_login.presentation.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Finalapp"
            tools:replace="android:exported">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deeplink -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="myapp" android:host="event"/>
            </intent-filter>
        </activity>

        <!-- FileProvider dùng share QR (từ master) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths_share" />
        </provider>

    </application>

</manifest>
